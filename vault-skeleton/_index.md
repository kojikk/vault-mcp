# _index — глобальный манифест

Манифест всех узлов и entity-страниц вольта. На каждую запись: путь, тип (`веха`/`кластер`/`проект`/`entity`), однострочное саммари, статус. Правится при изменении структуры (через `update_index`).

| Путь | Тип | Саммари | Статус |
|---|---|---|---|
| _(пусто)_ | — | вольт только что создан | — |
